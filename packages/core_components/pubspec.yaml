name: core_components
description: Core components package for projeto_flutter
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.2
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Dependências externas
  provider: ^6.1.1
  intl: ^0.19.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
